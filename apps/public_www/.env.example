# Admin HTTP API origin (no trailing slash). Used by NewsletterForm.
# Production CI sets this from the ADMIN_API_BASE_URL GitHub variable
# (same value as the admin SPA). Example:
# VITE_PUBLIC_API_URL=https://xxxxxxxxxx.execute-api.ap-southeast-1.amazonaws.com
VITE_PUBLIC_API_URL=
