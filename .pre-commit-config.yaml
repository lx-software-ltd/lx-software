# Pre-commit hooks configuration
# Install: brew install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: '\.md$'
      - id: end-of-file-fixer
        exclude: '\.md$'
      - id: check-yaml
        args: [--allow-multiple-documents]
      - id: check-json
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: check-merge-conflict
      - id: detect-private-key

  - repo: local
    hooks:
      - id: check-pii
        name: Check hashed PII denylist
        entry: bash scripts/check-pii.sh
        language: system
        pass_filenames: false
        always_run: true

  # TypeScript/JavaScript linting
  - repo: local
    hooks:
      - id: eslint-public-website
        name: ESLint Public Website
        entry: bash -c 'cd apps/public_www && npm run lint --if-present || true'
        language: system
        files: ^apps/public_www/.*\.(ts|tsx|js|jsx)$
        pass_filenames: false

# Configuration for specific tools
ci:
  autofix_commit_msg: "style: auto-fix by pre-commit hooks"
  autoupdate_commit_msg: "chore: update pre-commit hooks"
